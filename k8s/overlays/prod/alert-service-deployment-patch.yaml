apiVersion: apps/v1
kind: Deployment
metadata:
  name: realrisk-alert-service
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: alert-service
          resources:
            requests:
              cpu: 300m
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 1024Mi
