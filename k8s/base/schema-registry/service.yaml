apiVersion: v1
kind: Service
metadata:
  name: realrisk-schema-registry
spec:
  selector:
    app.kubernetes.io/name: realrisk-schema-registry
  ports:
    - name: http
      port: 8081
      targetPort: http
